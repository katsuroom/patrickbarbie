import TitleBar from "./TitleBar";
import StatusBar from "./StatusBar";

export default function MainScreen()
{
    return(
        <div>
            <TitleBar />
            <StatusBar />
        </div>
    );
}